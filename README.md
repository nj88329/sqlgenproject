Using this project we are using huggingface model to generate the sql queries by making useof the hugging face models in the streamlit app.
We can generate the sql queries using the sql database and make the assistant as a sql generator agent.

